"""Reproducible timings against scikit-learn.

The README claims a performance profile, and this is the script that produced
it. Install the extra and run it yourself rather than taking my word for it::

    pip install -e ".[benchmark]"
    python -m benchmarks.against_scikit_learn
"""
